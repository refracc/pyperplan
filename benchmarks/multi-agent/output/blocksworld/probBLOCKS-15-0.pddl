( define ( problem blocks-4-0 )
( :domain blocks )
( :objects
	a1 a2 a3 a4 - agent
	a c b e d g f i h k j m l o n - block
	acount-1 acount-2 acount-3 acount-4 - agent-count
)
( :init
	( handempty a1 )
	( handempty a2 )
	( handempty a3 )
	( handempty a4 )
	( clear e )
	( clear m )
	( clear b )
	( clear f )
	( clear i )
	( ontable g )
	( ontable n )
	( ontable o )
	( ontable k )
	( ontable h )
	( on e j )
	( on j d )
	( on d l )
	( on l c )
	( on c g )
	( on m n )
	( on b a )
	( on a o )
	( on f k )
	( on i h )
	( afree )
	( consec acount-0 acount-1 )
	( consec acount-1 acount-2 )
	( consec acount-2 acount-3 )
	( consec acount-3 acount-4 )
)
( :goal
	( and
		( on g o )
		( on o h )
		( on h k )
		( on k m )
		( on m f )
		( on f e )
		( on e a )
		( on a b )
		( on b l )
		( on l j )
		( on j d )
		( on d n )
		( on n i )
		( on i c )
		( afree )
	)
)
)
