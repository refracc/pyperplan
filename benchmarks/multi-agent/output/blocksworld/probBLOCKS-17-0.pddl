( define ( problem blocks-4-0 )
( :domain blocks )
( :objects
	a1 a2 a3 a4 - agent
	a c b e d g f i h k j m l o n q p - block
	acount-1 acount-2 acount-3 acount-4 - agent-count
)
( :init
	( handempty a1 )
	( handempty a2 )
	( handempty a3 )
	( handempty a4 )
	( clear q )
	( clear l )
	( clear g )
	( clear h )
	( clear p )
	( ontable m )
	( ontable k )
	( ontable o )
	( ontable n )
	( ontable p )
	( on q a )
	( on a j )
	( on j i )
	( on i b )
	( on b m )
	( on l f )
	( on f e )
	( on e k )
	( on g d )
	( on d c )
	( on c o )
	( on h n )
	( afree )
	( consec acount-0 acount-1 )
	( consec acount-1 acount-2 )
	( consec acount-2 acount-3 )
	( consec acount-3 acount-4 )
)
( :goal
	( and
		( on q n )
		( on n l )
		( on l o )
		( on o j )
		( on j h )
		( on h c )
		( on c e )
		( on e m )
		( on m p )
		( on p a )
		( on a g )
		( on g b )
		( on b i )
		( on i k )
		( on k f )
		( on f d )
		( afree )
	)
)
)
