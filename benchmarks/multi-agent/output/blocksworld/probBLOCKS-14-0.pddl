( define ( problem blocks-4-0 )
( :domain blocks )
( :objects
	a1 a2 a3 a4 - agent
	a c b e d g f i h k j m l n - block
	acount-1 acount-2 acount-3 acount-4 - agent-count
)
( :init
	( handempty a1 )
	( handempty a2 )
	( handempty a3 )
	( handempty a4 )
	( clear a )
	( clear g )
	( clear f )
	( ontable e )
	( ontable n )
	( ontable f )
	( on a j )
	( on j h )
	( on h m )
	( on m k )
	( on k c )
	( on c l )
	( on l b )
	( on b e )
	( on g d )
	( on d i )
	( on i n )
	( afree )
	( consec acount-0 acount-1 )
	( consec acount-1 acount-2 )
	( consec acount-2 acount-3 )
	( consec acount-3 acount-4 )
)
( :goal
	( and
		( on e l )
		( on l f )
		( on f b )
		( on b j )
		( on j i )
		( on i n )
		( on n c )
		( on c k )
		( on k g )
		( on g d )
		( on d m )
		( on m a )
		( on a h )
		( afree )
	)
)
)
