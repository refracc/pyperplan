( define ( problem blocks-4-0 )
( :domain blocks )
( :objects
	a1 a2 a3 a4 - agent
	a c b e d g f i h k j m l o n p - block
	acount-1 acount-2 acount-3 acount-4 - agent-count
)
( :init
	( handempty a1 )
	( handempty a2 )
	( handempty a3 )
	( handempty a4 )
	( clear e )
	( clear l )
	( ontable j )
	( ontable o )
	( on e f )
	( on f h )
	( on h b )
	( on b c )
	( on c m )
	( on m d )
	( on d a )
	( on a p )
	( on p n )
	( on n g )
	( on g i )
	( on i k )
	( on k j )
	( on l o )
	( afree )
	( consec acount-0 acount-1 )
	( consec acount-1 acount-2 )
	( consec acount-2 acount-3 )
	( consec acount-3 acount-4 )
)
( :goal
	( and
		( on i d )
		( on d h )
		( on h f )
		( on f b )
		( on b k )
		( on k j )
		( on j g )
		( on g e )
		( on e c )
		( on c l )
		( on l m )
		( on m n )
		( on n a )
		( on a p )
		( on p o )
		( afree )
	)
)
)
